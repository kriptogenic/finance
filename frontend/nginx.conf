server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml application/manifest+json;

    # Security headers for responses that don't override add_header (e.g. the SPA
    # fallback through `location /`). Locations below that set their own
    # add_header re-include the snippet, since nginx then stops inheriting these.
    include /etc/nginx/snippets/security-headers.conf;

    location /assets/ {
        include /etc/nginx/snippets/security-headers.conf;
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }

    location = /manifest.webmanifest {
        default_type application/manifest+json;
        try_files $uri =404;
    }

    # Never cache the service worker, so updates roll out immediately.
    location = /sw.js {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
        try_files $uri =404;
    }

    # Never cache the HTML shell; browsers must revalidate it on every load,
    # otherwise a stale index.html pins the app to old hashed assets.
    location = /index.html {
        include /etc/nginx/snippets/security-headers.conf;
        add_header Cache-Control "no-cache";
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
